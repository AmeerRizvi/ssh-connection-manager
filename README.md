# SSH Connection Manager

Manage your SSH connections directly in VS Code with a clean, expandable interface.

## How to use

1. Click the **+** button to add a new SSH connection
2. Enter a name and your SSH command (like `ssh user@server.com`)
3. Click the **→** arrow to connect, or expand the connection to rename/edit/delete
